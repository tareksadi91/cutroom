__pycache__/
*.pyc

# There is no media rule here because there is no media here, and there never
# will be: a project lives in ~/cutroom-projects/ and footage lives wherever
# the director keeps it. This repository holds a program. That separation is
# the whole point of the rewrite — read SPEC.md.
.gstack/

# Scratch workspace for subagent-driven-development execution (ledgers,
# task briefs, review packages) -- not part of the project.
.superpowers/
